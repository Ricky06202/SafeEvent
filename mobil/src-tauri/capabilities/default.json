{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capability for SafeEvent",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}